@model List<ManagementDashboard.Models.ListOfPendingClient>

<table class="table table-striped table-sm">
    <thead>
        <tr>
            <th>Reference</th>
            <th>Client</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Pending Status</th>
            <th>Service</th>
            <th>Sales Agent</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a class='mr-2' href='https://tpmsweb.threepeaks.co.za/Clients/Profile/ByRef/@item.Ref' target='_blank'><i class='fa fa-comment'></i></a>
                    @item.Ref
                </td>
                <td>@item.Customer</td>
                <td>@item.StartDate.ToString("dd-MM-yyyy")</td>
                <td>@item.EndDate.ToString("dd-MM-yyyy")</td>
                <td>@item.PendingStatus</td>
                <td>@item.Service</td>
                <td>@item.SalesAgent</td>
            </tr>
        }
    </tbody>
</table>
